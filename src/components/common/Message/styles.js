import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 42,
    borderRadius: 10,
    marginTop: 6,
    paddingHorizontal: 8,
    paddingVertical: 10,
  },
});
